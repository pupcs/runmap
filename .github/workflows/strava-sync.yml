name: Sync Strava Runs

on:
  schedule:
    - cron: '0 */6 * * *'  # Every 6 hours at minute 0
  workflow_dispatch:        # Allow manual trigger

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install requests gpxpy

      - name: Run sync script
        env:
          JANOS_CLIENT_ID: ${{ secrets.JANOS_CLIENT_ID }}
          JANOS_CLIENT_SECRET: ${{ secrets.JANOS_CLIENT_SECRET }}
          JANOS_REFRESH_TOKEN: ${{ secrets.JANOS_REFRESH_TOKEN }}
          JAZMIN_CLIENT_ID: ${{ secrets.JAZMIN_CLIENT_ID }}
          JAZMIN_CLIENT_SECRET: ${{ secrets.JAZMIN_CLIENT_SECRET }}
          JAZMIN_REFRESH_TOKEN: ${{ secrets.JAZMIN_REFRESH_TOKEN }}
        run: python strava_sync.py

      - name: Commit new run files
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add site/runs/
          git commit -m "Auto-sync Strava runs" || echo "No changes"
          git push
